{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/node",
      "config": {
        "buildCommand": "npm install --platform=linux --arch=x64 @rollup/rollup-linux-x64-gnu && npm run build"
      }
    }
  ]
}
